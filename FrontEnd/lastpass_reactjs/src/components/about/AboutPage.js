import React from "react";

const AboutPage = () => (
  <div>
    <h2>About</h2>
    <p>
      This app uses django and django-rest-framework
      for generating strong passwords in APIs
      React, Redux, React Router,React-Hooks and many other helpful
      libraries.
    </p>
  </div>
);

export default AboutPage;
